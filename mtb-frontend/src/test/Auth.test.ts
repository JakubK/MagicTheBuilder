import { mount } from '@vue/test-utils'
import { test, expect} from 'vitest';
import Auth from '../views/Auth.vue';

test('Auth starts on sign up', async () => {
});
  
test('Auth switch button redirects sign up <-> sign in', async () => {
});